---
- hosts: localhost
  #become: yes
  #become_user: root
  #gather_facts: no
  vars:
    vagrant: true
  roles:
    - { role: ../site-roles/cluster-init }
    #- { role: ../site-roles/ }
    #- { role: ../site-roles/ }
    #- { role: ../site-roles/ }
    #- { role: ../site-roles/ }

- hosts: swarm-cluster-nodes
  become: yes
  become_user: root 
  tasks:
    - name: 'say somthing im giving up on you'
      debug: var=playbook_dir
